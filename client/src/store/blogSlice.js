import { createSlice } from "@reduxjs/toolkit";

initialstate={
    status:false,
    blogData:null

}

const blogSlice=createSlice({
    name:'blog',
    initialState:initialstate,
    reducers:[{
        
    }]
})